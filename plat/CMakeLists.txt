IF(WIN32)
    add_subdirectory(windows)
    message("add_subdirectory windows")
ELSEIF(UNIX)
    IF(APPLE)
        add_subdirectory(apple)
    ELSEIF(BSD)
        add_subdirectory(linux)
    ELSE()
        add_subdirectory(linux)
    ENDIF(APPLE)
ELSE()
    add_subdirectory(empty)
ENDIF(WIN32)